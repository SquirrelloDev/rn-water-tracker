import {StyledText, StyledView} from "@/components/StyledComponents/StyledComponents";

export function SettingsFooter() {
    return (
        <StyledView>
            <StyledText>Sociale i app version</StyledText>
        </StyledView>
    );
}